import type { Metadata } from "next"; import { Callout, DocsLayout } from "@/components/docs-layout";
export const metadata: Metadata={title:"Usage",description:"Run LinSentry and read its section-by-section report."};
export default function Page(){return <DocsLayout><article className="max-w-3xl"><p className="text-sm font-medium text-zinc-600 dark:text-zinc-400">GETTING STARTED</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Usage</h1><p className="mt-6 text-lg leading-8 text-zinc-600 dark:text-zinc-400">Run LinSentry from the script directory. It prints a section-by-section report as it examines each area.</p><pre className="mt-8 rounded-lg bg-zinc-950 p-5 text-sm text-zinc-100"><code>$ ./linsentry.sh</code></pre><h2 className="mt-12 text-2xl font-semibold">Interactive changes</h2><p className="mt-4 leading-7 text-zinc-700 dark:text-zinc-300">When it finds a correctable issue, LinSentry asks for y/n confirmation before it modifies anything.</p><Callout>Run with sudo only when a check or approved fix needs protected system access.</Callout></article></DocsLayout>}

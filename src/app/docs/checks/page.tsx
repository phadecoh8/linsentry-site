import type { Metadata } from "next"; import { Callout, DocsLayout } from "@/components/docs-layout";
export const metadata: Metadata={title:"Security Checks",description:"Reference for LinSentry security hardening checks."};
const checks=[["Open port scanning","Lists active TCP and UDP listening ports."],["Port exposure detection","Flags ports bound to 0.0.0.0 or [::] rather than only local interfaces."],["SSH configuration audit","Checks PermitRootLogin, PasswordAuthentication, and PermitEmptyPasswords with interactive fixes."],["SSH config file permission audit","Verifies sshd_config is root-owned and not group/other writable."],["World-writable file scanner","Finds files any local user can modify, beginning in the home directory."],["User account audit","Flags duplicate UID 0 accounts and accounts with no password, offering interactive assignment."]];
export default function Page(){return <DocsLayout><article className="max-w-3xl"><p className="text-sm font-medium text-zinc-600 dark:text-zinc-400">REFERENCE</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Security checks</h1><div className="mt-12 space-y-10">{checks.map(([title,text],i)=><section key={title}><p className="font-mono text-xs text-zinc-500">{String(i+1).padStart(2,"0")}</p><h2 className="mt-2 text-2xl font-semibold">{title}</h2><p className="mt-3 leading-7 text-zinc-700 dark:text-zinc-300">{text} This helps you spot configurations that need deliberate review.</p></section>)}</div><Callout>Findings are prompts for review; always consider the host’s role and policy before making changes.</Callout></article></DocsLayout>}

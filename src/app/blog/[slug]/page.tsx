import type { Metadata } from "next"; import { notFound } from "next/navigation"; type Props={params:Promise<{slug:string}>}; export async function generateMetadata({params}:Props):Promise<Metadata>{const {slug}=await params;return slug==="welcome-to-linsentry"?{title:"Welcome to LinSentry",description:"An introduction to LinSentry, a security hardening auditor for Linux."}:{}} export default async function Page({params}:Props){const {slug}=await params;if(slug!=="welcome-to-linsentry")notFound();return <article className="mx-auto max-w-3xl px-5 py-16 sm:py-24"><p className="text-sm font-medium text-zinc-600 dark:text-zinc-400">PROJECT UPDATE · DATE TBA</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Welcome to LinSentry</h1><p className="mt-8 leading-8 text-zinc-700 dark:text-zinc-300">LinSentry helps you spot common hardening gaps quickly and make informed changes.</p></article>}

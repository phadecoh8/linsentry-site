import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const routes = ["", "/docs", "/docs/install", "/docs/usage", "/docs/checks", "/docs/scope", "/changelog", "/blog", "/blog/welcome-to-linsentry", "/blog/linsentry-v1-0"]; return routes.map((route) => ({ url: `https://linsentry.dev${route}`, lastModified: new Date(), changeFrequency: "weekly", priority: route === "" ? 1 : 0.7 })); }
